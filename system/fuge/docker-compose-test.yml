version: '3'
services:
  bases:
    build: ../../bases-service
    environment:
      HOST: bases
    command: bash -c "cd bases-service && npm test"
  api:
    build: ../../api-gateway
    ports:
        - "5000:5000"
    environment:
       HOST: api
       BASES: bases
    command: bash -c "cd api-gateway && npm test"
  ping:
    build: ../../ping-service
    environment:
       HOST: ping
       BASES: bases
    command: bash -c "cd ping-service && npm test"